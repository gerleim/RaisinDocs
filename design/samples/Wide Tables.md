# Wide tables

Generated by make-table-samples.py. Do not edit by hand.

## 1. Layout Docked

Section 0 describes how the layout docked transform interacts with the content measure document group. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `layout` and **window** once each.

Section 100 describes how the focused cache serializer interacts with the side layout docked transform. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `focused` and **panel** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 0 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 1 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 2 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 3 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 4 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 5 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 6 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 7 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 8 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 9 |

## 2. Content Measure

Section 1 describes how the content measure document interacts with the hidden offset focused cache. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `anchorable` and **serializer** once each.

Section 101 describes how the side layout docked interacts with the bitmap content measure document. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `activation` and **group** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 10 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 11 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 12 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 13 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 14 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 15 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 16 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 17 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 18 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 19 |

## 3. Hidden Offset

Section 2 describes how the hidden offset focused interacts with the arrange floating side layout. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `pane` and **content** once each.

Section 102 describes how the bitmap content measure interacts with the anchorable hidden offset focused. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `orientation` and **side** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | cache serializer element pane panel viewport | 20 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | layout docked transform selected render window remembered | 21 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | content measure document group margin orientation composite title | 22 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | hidden offset focused cache serializer element pane panel viewport | 23 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window. | arrange floating side layout docked transform selected render window remembered | 24 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | viewport activation bitmap content measure document | 25 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | window remembered anchorable hidden offset focused cache | 26 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | orientation composite title arrange floating side layout docked | 27 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | element pane panel viewport activation bitmap content measure document | 28 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | transform selected render window remembered anchorable hidden offset focused cache | 29 |

## 4. Arrange Floating

Section 3 describes how the arrange floating side interacts with the viewport activation bitmap content. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `document` and **title** once each.

Section 103 describes how the anchorable hidden offset interacts with the title arrange floating side. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `docked` and **remembered** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | document group margin orientation composite title | 30 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | focused cache serializer element pane panel viewport | 31 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | side layout docked transform selected render window remembered | 32 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | bitmap content measure document group margin orientation composite title | 33 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | anchorable hidden offset focused cache serializer element pane panel viewport | 34 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | title arrange floating side layout docked | 35 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | panel viewport activation bitmap content measure document | 36 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | render window remembered anchorable hidden offset focused cache | 37 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | margin orientation composite title arrange floating side layout docked | 38 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | serializer element pane panel viewport activation bitmap content measure document | 39 |

## 5. Viewport Activation

Section 4 describes how the viewport activation bitmap interacts with the window remembered anchorable hidden. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `floating` and **selected** once each.

Section 104 describes how the title arrange floating interacts with the panel viewport activation bitmap. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `hidden` and **element** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | docked transform selected render window remembered | 40 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | measure document group margin orientation composite title | 41 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | offset focused cache serializer element pane panel viewport | 42 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | floating side layout docked transform selected render window remembered | 43 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | activation bitmap content measure document group margin orientation composite title | 44 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | remembered anchorable hidden offset focused cache | 45 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | composite title arrange floating side layout docked | 46 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | pane panel viewport activation bitmap content measure document | 47 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | selected render window remembered anchorable hidden offset focused cache | 48 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | group margin orientation composite title arrange floating side layout docked | 49 |

## 6. Window Remembered

Section 5 describes how the window remembered anchorable interacts with the orientation composite title arrange. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `window` and **focused** once each.

Section 105 describes how the panel viewport activation interacts with the render window remembered anchorable. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `panel` and **measure** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | cache serializer element pane panel viewport | 50 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | layout docked transform selected render window remembered | 51 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | content measure document group margin orientation composite title | 52 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | hidden offset focused cache serializer element pane panel viewport | 53 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | arrange floating side layout docked transform selected render window remembered | 54 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | viewport activation bitmap content measure document | 55 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | window remembered anchorable hidden offset focused cache | 56 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | orientation composite title arrange floating side layout docked | 57 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | element pane panel viewport activation bitmap content measure document | 58 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | transform selected render window remembered anchorable hidden offset focused cache | 59 |

## 7. Orientation Composite

Section 6 describes how the orientation composite title interacts with the element pane panel viewport. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `serializer` and **activation** once each.

Section 106 describes how the render window remembered interacts with the margin orientation composite title. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `group` and **arrange** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 60 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 61 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 62 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 63 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 64 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 65 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 66 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 67 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 68 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 69 |

## 8. Element Pane

Section 7 describes how the element pane panel interacts with the transform selected render window. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `content` and **orientation** once each.

Section 107 describes how the margin orientation composite interacts with the serializer element pane panel. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `side` and **render** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 70 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 71 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 72 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 73 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 74 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 75 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 76 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 77 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 78 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 79 |

## 9. Transform Selected

Section 8 describes how the transform selected render interacts with the document group margin orientation. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `title` and **docked** once each.

Section 108 describes how the serializer element pane interacts with the docked transform selected render. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `remembered` and **cache** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | cache serializer element pane panel viewport | 80 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | layout docked transform selected render window remembered | 81 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | content measure document group margin orientation composite title | 82 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | hidden offset focused cache serializer element pane panel viewport | 83 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window. | arrange floating side layout docked transform selected render window remembered | 84 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | viewport activation bitmap content measure document | 85 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | window remembered anchorable hidden offset focused cache | 86 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | orientation composite title arrange floating side layout docked | 87 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | element pane panel viewport activation bitmap content measure document | 88 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | transform selected render window remembered anchorable hidden offset focused cache | 89 |

## 10. Document Group

Section 9 describes how the document group margin interacts with the focused cache serializer element. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `selected` and **hidden** once each.

Section 109 describes how the docked transform selected interacts with the measure document group margin. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `element` and **bitmap** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | document group margin orientation composite title | 90 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | focused cache serializer element pane panel viewport | 91 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | side layout docked transform selected render window remembered | 92 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | bitmap content measure document group margin orientation composite title | 93 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | anchorable hidden offset focused cache serializer element pane panel viewport | 94 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | title arrange floating side layout docked | 95 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | panel viewport activation bitmap content measure document | 96 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | render window remembered anchorable hidden offset focused cache | 0 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | margin orientation composite title arrange floating side layout docked | 1 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | serializer element pane panel viewport activation bitmap content measure document | 2 |

## 11. Focused Cache

Section 10 describes how the focused cache serializer interacts with the side layout docked transform. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `focused` and **panel** once each.

Section 110 describes how the measure document group interacts with the offset focused cache serializer. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `measure` and **composite** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | docked transform selected render window remembered | 3 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | measure document group margin orientation composite title | 4 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | offset focused cache serializer element pane panel viewport | 5 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | floating side layout docked transform selected render window remembered | 6 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | activation bitmap content measure document group margin orientation composite title | 7 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | remembered anchorable hidden offset focused cache | 8 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | composite title arrange floating side layout docked | 9 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | pane panel viewport activation bitmap content measure document | 10 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | selected render window remembered anchorable hidden offset focused cache | 11 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | group margin orientation composite title arrange floating side layout docked | 12 |

## 12. Side Layout

Section 11 describes how the side layout docked interacts with the bitmap content measure document. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `activation` and **group** once each.

Section 111 describes how the offset focused cache interacts with the floating side layout docked. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `arrange` and **transform** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | cache serializer element pane panel viewport | 13 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | layout docked transform selected render window remembered | 14 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | content measure document group margin orientation composite title | 15 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | hidden offset focused cache serializer element pane panel viewport | 16 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | arrange floating side layout docked transform selected render window remembered | 17 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | viewport activation bitmap content measure document | 18 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | window remembered anchorable hidden offset focused cache | 19 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | orientation composite title arrange floating side layout docked | 20 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | element pane panel viewport activation bitmap content measure document | 21 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | transform selected render window remembered anchorable hidden offset focused cache | 22 |

## 13. Bitmap Content

Section 12 describes how the bitmap content measure interacts with the anchorable hidden offset focused. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `orientation` and **side** once each.

Section 112 describes how the floating side layout interacts with the activation bitmap content measure. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `render` and **offset** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 23 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 24 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 25 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 26 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 27 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 28 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 29 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 30 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 31 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 32 |

## 14. Anchorable Hidden

Section 13 describes how the anchorable hidden offset interacts with the title arrange floating side. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `docked` and **remembered** once each.

Section 113 describes how the activation bitmap content interacts with the remembered anchorable hidden offset. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `cache` and **viewport** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 33 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 34 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 35 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 36 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 37 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 38 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 39 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 40 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 41 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 42 |

## 15. Title Arrange

Section 14 describes how the title arrange floating interacts with the panel viewport activation bitmap. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `hidden` and **element** once each.

Section 114 describes how the remembered anchorable hidden interacts with the composite title arrange floating. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `bitmap` and **margin** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | cache serializer element pane panel viewport | 43 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | layout docked transform selected render window remembered | 44 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | content measure document group margin orientation composite title | 45 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | hidden offset focused cache serializer element pane panel viewport | 46 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window. | arrange floating side layout docked transform selected render window remembered | 47 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | viewport activation bitmap content measure document | 48 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | window remembered anchorable hidden offset focused cache | 49 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | orientation composite title arrange floating side layout docked | 50 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | element pane panel viewport activation bitmap content measure document | 51 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | transform selected render window remembered anchorable hidden offset focused cache | 52 |

## 16. Panel Viewport

Section 15 describes how the panel viewport activation interacts with the render window remembered anchorable. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `panel` and **measure** once each.

Section 115 describes how the composite title arrange interacts with the pane panel viewport activation. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `composite` and **layout** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | document group margin orientation composite title | 53 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | focused cache serializer element pane panel viewport | 54 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | side layout docked transform selected render window remembered | 55 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | bitmap content measure document group margin orientation composite title | 56 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | anchorable hidden offset focused cache serializer element pane panel viewport | 57 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | title arrange floating side layout docked | 58 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | panel viewport activation bitmap content measure document | 59 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | render window remembered anchorable hidden offset focused cache | 60 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | margin orientation composite title arrange floating side layout docked | 61 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | serializer element pane panel viewport activation bitmap content measure document | 62 |

## 17. Render Window

Section 16 describes how the render window remembered interacts with the margin orientation composite title. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `group` and **arrange** once each.

Section 116 describes how the pane panel viewport interacts with the selected render window remembered. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `transform` and **anchorable** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | docked transform selected render window remembered | 63 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | measure document group margin orientation composite title | 64 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | offset focused cache serializer element pane panel viewport | 65 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | floating side layout docked transform selected render window remembered | 66 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | activation bitmap content measure document group margin orientation composite title | 67 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | remembered anchorable hidden offset focused cache | 68 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | composite title arrange floating side layout docked | 69 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | pane panel viewport activation bitmap content measure document | 70 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | selected render window remembered anchorable hidden offset focused cache | 71 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | group margin orientation composite title arrange floating side layout docked | 72 |

## 18. Margin Orientation

Section 17 describes how the margin orientation composite interacts with the serializer element pane panel. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `side` and **render** once each.

Section 117 describes how the selected render window interacts with the group margin orientation composite. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `offset` and **pane** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | cache serializer element pane panel viewport | 73 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | layout docked transform selected render window remembered | 74 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | content measure document group margin orientation composite title | 75 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | hidden offset focused cache serializer element pane panel viewport | 76 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | arrange floating side layout docked transform selected render window remembered | 77 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | viewport activation bitmap content measure document | 78 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | window remembered anchorable hidden offset focused cache | 79 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | orientation composite title arrange floating side layout docked | 80 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | element pane panel viewport activation bitmap content measure document | 81 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | transform selected render window remembered anchorable hidden offset focused cache | 82 |

## 19. Serializer Element

Section 18 describes how the serializer element pane interacts with the docked transform selected render. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `remembered` and **cache** once each.

Section 118 describes how the group margin orientation interacts with the cache serializer element pane. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `viewport` and **document** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 83 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 84 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 85 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 86 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 87 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 88 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 89 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 90 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 91 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 92 |

## 20. Docked Transform

Section 19 describes how the docked transform selected interacts with the measure document group margin. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `element` and **bitmap** once each.

Section 119 describes how the cache serializer element interacts with the layout docked transform selected. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `margin` and **floating** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 93 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 94 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 95 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 96 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 0 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 1 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 2 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 3 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 4 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 5 |

## 21. Measure Document

Section 20 describes how the measure document group interacts with the offset focused cache serializer. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `measure` and **composite** once each.

Section 120 describes how the layout docked transform interacts with the content measure document group. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `layout` and **window** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | cache serializer element pane panel viewport | 6 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | layout docked transform selected render window remembered | 7 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | content measure document group margin orientation composite title | 8 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | hidden offset focused cache serializer element pane panel viewport | 9 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window. | arrange floating side layout docked transform selected render window remembered | 10 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | viewport activation bitmap content measure document | 11 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | window remembered anchorable hidden offset focused cache | 12 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | orientation composite title arrange floating side layout docked | 13 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | element pane panel viewport activation bitmap content measure document | 14 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | transform selected render window remembered anchorable hidden offset focused cache | 15 |

## 22. Offset Focused

Section 21 describes how the offset focused cache interacts with the floating side layout docked. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `arrange` and **transform** once each.

Section 121 describes how the content measure document interacts with the hidden offset focused cache. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `anchorable` and **serializer** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | document group margin orientation composite title | 16 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | focused cache serializer element pane panel viewport | 17 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | side layout docked transform selected render window remembered | 18 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | bitmap content measure document group margin orientation composite title | 19 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | anchorable hidden offset focused cache serializer element pane panel viewport | 20 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | title arrange floating side layout docked | 21 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | panel viewport activation bitmap content measure document | 22 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | render window remembered anchorable hidden offset focused cache | 23 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | margin orientation composite title arrange floating side layout docked | 24 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | serializer element pane panel viewport activation bitmap content measure document | 25 |

## 23. Floating Side

Section 22 describes how the floating side layout interacts with the activation bitmap content measure. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `render` and **offset** once each.

Section 122 describes how the hidden offset focused interacts with the arrange floating side layout. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `pane` and **content** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | docked transform selected render window remembered | 26 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | measure document group margin orientation composite title | 27 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | offset focused cache serializer element pane panel viewport | 28 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | floating side layout docked transform selected render window remembered | 29 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | activation bitmap content measure document group margin orientation composite title | 30 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | remembered anchorable hidden offset focused cache | 31 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | composite title arrange floating side layout docked | 32 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | pane panel viewport activation bitmap content measure document | 33 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | selected render window remembered anchorable hidden offset focused cache | 34 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | group margin orientation composite title arrange floating side layout docked | 35 |

## 24. Activation Bitmap

Section 23 describes how the activation bitmap content interacts with the remembered anchorable hidden offset. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `cache` and **viewport** once each.

Section 123 describes how the arrange floating side interacts with the viewport activation bitmap content. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `document` and **title** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | cache serializer element pane panel viewport | 36 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | layout docked transform selected render window remembered | 37 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | content measure document group margin orientation composite title | 38 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | hidden offset focused cache serializer element pane panel viewport | 39 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | arrange floating side layout docked transform selected render window remembered | 40 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | viewport activation bitmap content measure document | 41 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | window remembered anchorable hidden offset focused cache | 42 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | orientation composite title arrange floating side layout docked | 43 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | element pane panel viewport activation bitmap content measure document | 44 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | transform selected render window remembered anchorable hidden offset focused cache | 45 |

## 25. Remembered Anchorable

Section 24 describes how the remembered anchorable hidden interacts with the composite title arrange floating. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `bitmap` and **margin** once each.

Section 124 describes how the viewport activation bitmap interacts with the window remembered anchorable hidden. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `floating` and **selected** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 46 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 47 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 48 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 49 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 50 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 51 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 52 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 53 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 54 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 55 |

## 26. Composite Title

Section 25 describes how the composite title arrange interacts with the pane panel viewport activation. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `composite` and **layout** once each.

Section 125 describes how the window remembered anchorable interacts with the orientation composite title arrange. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `window` and **focused** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 56 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 57 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 58 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 59 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 60 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 61 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 62 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 63 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 64 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 65 |

## 27. Pane Panel

Section 26 describes how the pane panel viewport interacts with the selected render window remembered. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `transform` and **anchorable** once each.

Section 126 describes how the orientation composite title interacts with the element pane panel viewport. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `serializer` and **activation** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | cache serializer element pane panel viewport | 66 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | layout docked transform selected render window remembered | 67 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | content measure document group margin orientation composite title | 68 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | hidden offset focused cache serializer element pane panel viewport | 69 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window. | arrange floating side layout docked transform selected render window remembered | 70 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | viewport activation bitmap content measure document | 71 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | window remembered anchorable hidden offset focused cache | 72 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | orientation composite title arrange floating side layout docked | 73 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | element pane panel viewport activation bitmap content measure document | 74 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | transform selected render window remembered anchorable hidden offset focused cache | 75 |

## 28. Selected Render

Section 27 describes how the selected render window interacts with the group margin orientation composite. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `offset` and **pane** once each.

Section 127 describes how the element pane panel interacts with the transform selected render window. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `content` and **orientation** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | document group margin orientation composite title | 76 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | focused cache serializer element pane panel viewport | 77 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | side layout docked transform selected render window remembered | 78 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | bitmap content measure document group margin orientation composite title | 79 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | anchorable hidden offset focused cache serializer element pane panel viewport | 80 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | title arrange floating side layout docked | 81 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | panel viewport activation bitmap content measure document | 82 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | render window remembered anchorable hidden offset focused cache | 83 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | margin orientation composite title arrange floating side layout docked | 84 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | serializer element pane panel viewport activation bitmap content measure document | 85 |

## 29. Group Margin

Section 28 describes how the group margin orientation interacts with the cache serializer element pane. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `viewport` and **document** once each.

Section 128 describes how the transform selected render interacts with the document group margin orientation. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `title` and **docked** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | docked transform selected render window remembered | 86 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | measure document group margin orientation composite title | 87 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | offset focused cache serializer element pane panel viewport | 88 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | floating side layout docked transform selected render window remembered | 89 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | activation bitmap content measure document group margin orientation composite title | 90 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | remembered anchorable hidden offset focused cache | 91 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | composite title arrange floating side layout docked | 92 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | pane panel viewport activation bitmap content measure document | 93 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | selected render window remembered anchorable hidden offset focused cache | 94 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | group margin orientation composite title arrange floating side layout docked | 95 |

## 30. Cache Serializer

Section 29 describes how the cache serializer element interacts with the layout docked transform selected. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `margin` and **floating** once each.

Section 129 describes how the document group margin interacts with the focused cache serializer element. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `selected` and **hidden** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | cache serializer element pane panel viewport | 96 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | layout docked transform selected render window remembered | 0 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | content measure document group margin orientation composite title | 1 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | hidden offset focused cache serializer element pane panel viewport | 2 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | arrange floating side layout docked transform selected render window remembered | 3 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | viewport activation bitmap content measure document | 4 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | window remembered anchorable hidden offset focused cache | 5 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | orientation composite title arrange floating side layout docked | 6 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | element pane panel viewport activation bitmap content measure document | 7 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | transform selected render window remembered anchorable hidden offset focused cache | 8 |

## 31. Layout Docked

Section 30 describes how the layout docked transform interacts with the content measure document group. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `layout` and **window** once each.

Section 130 describes how the focused cache serializer interacts with the side layout docked transform. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `focused` and **panel** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 9 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 10 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 11 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 12 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 13 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 14 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 15 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 16 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 17 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 18 |

## 32. Content Measure

Section 31 describes how the content measure document interacts with the hidden offset focused cache. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `anchorable` and **serializer** once each.

Section 131 describes how the side layout docked interacts with the bitmap content measure document. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `activation` and **group** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 19 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 20 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 21 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 22 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 23 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 24 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 25 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 26 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 27 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 28 |

## 33. Hidden Offset

Section 32 describes how the hidden offset focused interacts with the arrange floating side layout. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `pane` and **content** once each.

Section 132 describes how the bitmap content measure interacts with the anchorable hidden offset focused. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `orientation` and **side** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | cache serializer element pane panel viewport | 29 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | layout docked transform selected render window remembered | 30 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | content measure document group margin orientation composite title | 31 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | hidden offset focused cache serializer element pane panel viewport | 32 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window. | arrange floating side layout docked transform selected render window remembered | 33 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | viewport activation bitmap content measure document | 34 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | window remembered anchorable hidden offset focused cache | 35 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | orientation composite title arrange floating side layout docked | 36 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | element pane panel viewport activation bitmap content measure document | 37 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | transform selected render window remembered anchorable hidden offset focused cache | 38 |

## 34. Arrange Floating

Section 33 describes how the arrange floating side interacts with the viewport activation bitmap content. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `document` and **title** once each.

Section 133 describes how the anchorable hidden offset interacts with the title arrange floating side. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `docked` and **remembered** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | document group margin orientation composite title | 39 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | focused cache serializer element pane panel viewport | 40 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | side layout docked transform selected render window remembered | 41 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | bitmap content measure document group margin orientation composite title | 42 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | anchorable hidden offset focused cache serializer element pane panel viewport | 43 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | title arrange floating side layout docked | 44 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | panel viewport activation bitmap content measure document | 45 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | render window remembered anchorable hidden offset focused cache | 46 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | margin orientation composite title arrange floating side layout docked | 47 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | serializer element pane panel viewport activation bitmap content measure document | 48 |

## 35. Viewport Activation

Section 34 describes how the viewport activation bitmap interacts with the window remembered anchorable hidden. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `floating` and **selected** once each.

Section 134 describes how the title arrange floating interacts with the panel viewport activation bitmap. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `hidden` and **element** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | docked transform selected render window remembered | 49 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | measure document group margin orientation composite title | 50 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | offset focused cache serializer element pane panel viewport | 51 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | floating side layout docked transform selected render window remembered | 52 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | activation bitmap content measure document group margin orientation composite title | 53 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | remembered anchorable hidden offset focused cache | 54 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | composite title arrange floating side layout docked | 55 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | pane panel viewport activation bitmap content measure document | 56 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | selected render window remembered anchorable hidden offset focused cache | 57 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | group margin orientation composite title arrange floating side layout docked | 58 |

## 36. Window Remembered

Section 35 describes how the window remembered anchorable interacts with the orientation composite title arrange. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `window` and **focused** once each.

Section 135 describes how the panel viewport activation interacts with the render window remembered anchorable. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `panel` and **measure** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | cache serializer element pane panel viewport | 59 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | layout docked transform selected render window remembered | 60 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | content measure document group margin orientation composite title | 61 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | hidden offset focused cache serializer element pane panel viewport | 62 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | arrange floating side layout docked transform selected render window remembered | 63 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | viewport activation bitmap content measure document | 64 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | window remembered anchorable hidden offset focused cache | 65 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | orientation composite title arrange floating side layout docked | 66 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | element pane panel viewport activation bitmap content measure document | 67 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | transform selected render window remembered anchorable hidden offset focused cache | 68 |

## 37. Orientation Composite

Section 36 describes how the orientation composite title interacts with the element pane panel viewport. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `serializer` and **activation** once each.

Section 136 describes how the render window remembered interacts with the margin orientation composite title. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `group` and **arrange** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 69 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 70 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 71 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 72 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 73 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 74 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 75 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 76 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 77 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 78 |

## 38. Element Pane

Section 37 describes how the element pane panel interacts with the transform selected render window. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `content` and **orientation** once each.

Section 137 describes how the margin orientation composite interacts with the serializer element pane panel. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `side` and **render** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 79 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 80 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 81 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 82 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 83 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 84 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 85 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 86 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 87 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 88 |

## 39. Transform Selected

Section 38 describes how the transform selected render interacts with the document group margin orientation. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `title` and **docked** once each.

Section 138 describes how the serializer element pane interacts with the docked transform selected render. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `remembered` and **cache** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | cache serializer element pane panel viewport | 89 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | layout docked transform selected render window remembered | 90 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | content measure document group margin orientation composite title | 91 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | hidden offset focused cache serializer element pane panel viewport | 92 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window. | arrange floating side layout docked transform selected render window remembered | 93 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | viewport activation bitmap content measure document | 94 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | window remembered anchorable hidden offset focused cache | 95 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | orientation composite title arrange floating side layout docked | 96 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | element pane panel viewport activation bitmap content measure document | 0 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | transform selected render window remembered anchorable hidden offset focused cache | 1 |

## 40. Document Group

Section 39 describes how the document group margin interacts with the focused cache serializer element. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `selected` and **hidden** once each.

Section 139 describes how the docked transform selected interacts with the measure document group margin. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `element` and **bitmap** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | document group margin orientation composite title | 2 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | focused cache serializer element pane panel viewport | 3 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | side layout docked transform selected render window remembered | 4 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | bitmap content measure document group margin orientation composite title | 5 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | anchorable hidden offset focused cache serializer element pane panel viewport | 6 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | title arrange floating side layout docked | 7 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | panel viewport activation bitmap content measure document | 8 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | render window remembered anchorable hidden offset focused cache | 9 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | margin orientation composite title arrange floating side layout docked | 10 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | serializer element pane panel viewport activation bitmap content measure document | 11 |

## 41. Focused Cache

Section 40 describes how the focused cache serializer interacts with the side layout docked transform. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `focused` and **panel** once each.

Section 140 describes how the measure document group interacts with the offset focused cache serializer. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `measure` and **composite** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | docked transform selected render window remembered | 12 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | measure document group margin orientation composite title | 13 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | offset focused cache serializer element pane panel viewport | 14 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | floating side layout docked transform selected render window remembered | 15 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | activation bitmap content measure document group margin orientation composite title | 16 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | remembered anchorable hidden offset focused cache | 17 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | composite title arrange floating side layout docked | 18 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | pane panel viewport activation bitmap content measure document | 19 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | selected render window remembered anchorable hidden offset focused cache | 20 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | group margin orientation composite title arrange floating side layout docked | 21 |

## 42. Side Layout

Section 41 describes how the side layout docked interacts with the bitmap content measure document. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `activation` and **group** once each.

Section 141 describes how the offset focused cache interacts with the floating side layout docked. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `arrange` and **transform** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | cache serializer element pane panel viewport | 22 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | layout docked transform selected render window remembered | 23 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | content measure document group margin orientation composite title | 24 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | hidden offset focused cache serializer element pane panel viewport | 25 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | arrange floating side layout docked transform selected render window remembered | 26 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | viewport activation bitmap content measure document | 27 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | window remembered anchorable hidden offset focused cache | 28 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | orientation composite title arrange floating side layout docked | 29 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | element pane panel viewport activation bitmap content measure document | 30 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | transform selected render window remembered anchorable hidden offset focused cache | 31 |

## 43. Bitmap Content

Section 42 describes how the bitmap content measure interacts with the anchorable hidden offset focused. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `orientation` and **side** once each.

Section 142 describes how the floating side layout interacts with the activation bitmap content measure. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `render` and **offset** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 32 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 33 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 34 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 35 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 36 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 37 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 38 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 39 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 40 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 41 |

## 44. Anchorable Hidden

Section 43 describes how the anchorable hidden offset interacts with the title arrange floating side. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `docked` and **remembered** once each.

Section 143 describes how the activation bitmap content interacts with the remembered anchorable hidden offset. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `cache` and **viewport** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 42 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 43 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 44 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 45 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 46 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 47 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 48 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 49 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 50 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 51 |

## 45. Title Arrange

Section 44 describes how the title arrange floating interacts with the panel viewport activation bitmap. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `hidden` and **element** once each.

Section 144 describes how the remembered anchorable hidden interacts with the composite title arrange floating. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `bitmap` and **margin** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | cache serializer element pane panel viewport | 52 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | layout docked transform selected render window remembered | 53 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | content measure document group margin orientation composite title | 54 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | hidden offset focused cache serializer element pane panel viewport | 55 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window. | arrange floating side layout docked transform selected render window remembered | 56 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | viewport activation bitmap content measure document | 57 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | window remembered anchorable hidden offset focused cache | 58 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | orientation composite title arrange floating side layout docked | 59 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | element pane panel viewport activation bitmap content measure document | 60 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | transform selected render window remembered anchorable hidden offset focused cache | 61 |

## 46. Panel Viewport

Section 45 describes how the panel viewport activation interacts with the render window remembered anchorable. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `panel` and **measure** once each.

Section 145 describes how the composite title arrange interacts with the pane panel viewport activation. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `composite` and **layout** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | document group margin orientation composite title | 62 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | focused cache serializer element pane panel viewport | 63 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | side layout docked transform selected render window remembered | 64 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | bitmap content measure document group margin orientation composite title | 65 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | anchorable hidden offset focused cache serializer element pane panel viewport | 66 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | title arrange floating side layout docked | 67 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | panel viewport activation bitmap content measure document | 68 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | render window remembered anchorable hidden offset focused cache | 69 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | margin orientation composite title arrange floating side layout docked | 70 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | serializer element pane panel viewport activation bitmap content measure document | 71 |

## 47. Render Window

Section 46 describes how the render window remembered interacts with the margin orientation composite title. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `group` and **arrange** once each.

Section 146 describes how the pane panel viewport interacts with the selected render window remembered. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `transform` and **anchorable** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | docked transform selected render window remembered | 72 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | measure document group margin orientation composite title | 73 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | offset focused cache serializer element pane panel viewport | 74 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | floating side layout docked transform selected render window remembered | 75 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | activation bitmap content measure document group margin orientation composite title | 76 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | remembered anchorable hidden offset focused cache | 77 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | composite title arrange floating side layout docked | 78 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | pane panel viewport activation bitmap content measure document | 79 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | selected render window remembered anchorable hidden offset focused cache | 80 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | group margin orientation composite title arrange floating side layout docked | 81 |

## 48. Margin Orientation

Section 47 describes how the margin orientation composite interacts with the serializer element pane panel. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `side` and **render** once each.

Section 147 describes how the selected render window interacts with the group margin orientation composite. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `offset` and **pane** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | cache serializer element pane panel viewport | 82 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | layout docked transform selected render window remembered | 83 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | content measure document group margin orientation composite title | 84 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | hidden offset focused cache serializer element pane panel viewport | 85 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | arrange floating side layout docked transform selected render window remembered | 86 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | viewport activation bitmap content measure document | 87 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | window remembered anchorable hidden offset focused cache | 88 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | orientation composite title arrange floating side layout docked | 89 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | element pane panel viewport activation bitmap content measure document | 90 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | transform selected render window remembered anchorable hidden offset focused cache | 91 |

## 49. Serializer Element

Section 48 describes how the serializer element pane interacts with the docked transform selected render. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `remembered` and **cache** once each.

Section 148 describes how the group margin orientation interacts with the cache serializer element pane. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `viewport` and **document** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 92 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 93 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 94 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 95 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 96 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 0 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 1 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 2 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 3 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 4 |

## 50. Docked Transform

Section 49 describes how the docked transform selected interacts with the measure document group margin. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `element` and **bitmap** once each.

Section 149 describes how the cache serializer element interacts with the layout docked transform selected. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `margin` and **floating** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 5 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 6 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 7 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 8 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 9 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 10 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 11 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 12 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 13 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 14 |

## 51. Measure Document

Section 50 describes how the measure document group interacts with the offset focused cache serializer. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `measure` and **composite** once each.

Section 150 describes how the layout docked transform interacts with the content measure document group. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `layout` and **window** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | cache serializer element pane panel viewport | 15 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | layout docked transform selected render window remembered | 16 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | content measure document group margin orientation composite title | 17 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | hidden offset focused cache serializer element pane panel viewport | 18 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window. | arrange floating side layout docked transform selected render window remembered | 19 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | viewport activation bitmap content measure document | 20 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | window remembered anchorable hidden offset focused cache | 21 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | orientation composite title arrange floating side layout docked | 22 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | element pane panel viewport activation bitmap content measure document | 23 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | transform selected render window remembered anchorable hidden offset focused cache | 24 |

## 52. Offset Focused

Section 51 describes how the offset focused cache interacts with the floating side layout docked. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `arrange` and **transform** once each.

Section 151 describes how the content measure document interacts with the hidden offset focused cache. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `anchorable` and **serializer** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | document group margin orientation composite title | 25 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | focused cache serializer element pane panel viewport | 26 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | side layout docked transform selected render window remembered | 27 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | bitmap content measure document group margin orientation composite title | 28 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | anchorable hidden offset focused cache serializer element pane panel viewport | 29 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | title arrange floating side layout docked | 30 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | panel viewport activation bitmap content measure document | 31 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | render window remembered anchorable hidden offset focused cache | 32 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | margin orientation composite title arrange floating side layout docked | 33 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | serializer element pane panel viewport activation bitmap content measure document | 34 |

## 53. Floating Side

Section 52 describes how the floating side layout interacts with the activation bitmap content measure. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `render` and **offset** once each.

Section 152 describes how the hidden offset focused interacts with the arrange floating side layout. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `pane` and **content** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | docked transform selected render window remembered | 35 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | measure document group margin orientation composite title | 36 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | offset focused cache serializer element pane panel viewport | 37 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | floating side layout docked transform selected render window remembered | 38 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | activation bitmap content measure document group margin orientation composite title | 39 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | remembered anchorable hidden offset focused cache | 40 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | composite title arrange floating side layout docked | 41 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | pane panel viewport activation bitmap content measure document | 42 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | selected render window remembered anchorable hidden offset focused cache | 43 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | group margin orientation composite title arrange floating side layout docked | 44 |

## 54. Activation Bitmap

Section 53 describes how the activation bitmap content interacts with the remembered anchorable hidden offset. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `cache` and **viewport** once each.

Section 153 describes how the arrange floating side interacts with the viewport activation bitmap content. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `document` and **title** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | cache serializer element pane panel viewport | 45 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | layout docked transform selected render window remembered | 46 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | content measure document group margin orientation composite title | 47 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | hidden offset focused cache serializer element pane panel viewport | 48 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | arrange floating side layout docked transform selected render window remembered | 49 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | viewport activation bitmap content measure document | 50 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | window remembered anchorable hidden offset focused cache | 51 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | orientation composite title arrange floating side layout docked | 52 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | element pane panel viewport activation bitmap content measure document | 53 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | transform selected render window remembered anchorable hidden offset focused cache | 54 |

## 55. Remembered Anchorable

Section 54 describes how the remembered anchorable hidden interacts with the composite title arrange floating. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `bitmap` and **margin** once each.

Section 154 describes how the viewport activation bitmap interacts with the window remembered anchorable hidden. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `floating` and **selected** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 55 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 56 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 57 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 58 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 59 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 60 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 61 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 62 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 63 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 64 |

## 56. Composite Title

Section 55 describes how the composite title arrange interacts with the pane panel viewport activation. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `composite` and **layout** once each.

Section 155 describes how the window remembered anchorable interacts with the orientation composite title arrange. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `window` and **focused** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 65 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 66 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 67 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 68 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 69 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 70 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 71 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 72 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 73 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 74 |

## 57. Pane Panel

Section 56 describes how the pane panel viewport interacts with the selected render window remembered. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `transform` and **anchorable** once each.

Section 156 describes how the orientation composite title interacts with the element pane panel viewport. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `serializer` and **activation** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | cache serializer element pane panel viewport | 75 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | layout docked transform selected render window remembered | 76 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | content measure document group margin orientation composite title | 77 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | hidden offset focused cache serializer element pane panel viewport | 78 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window. | arrange floating side layout docked transform selected render window remembered | 79 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | viewport activation bitmap content measure document | 80 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | window remembered anchorable hidden offset focused cache | 81 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | orientation composite title arrange floating side layout docked | 82 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | element pane panel viewport activation bitmap content measure document | 83 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | transform selected render window remembered anchorable hidden offset focused cache | 84 |

## 58. Selected Render

Section 57 describes how the selected render window interacts with the group margin orientation composite. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `offset` and **pane** once each.

Section 157 describes how the element pane panel interacts with the transform selected render window. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `content` and **orientation** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | document group margin orientation composite title | 85 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | focused cache serializer element pane panel viewport | 86 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | side layout docked transform selected render window remembered | 87 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | bitmap content measure document group margin orientation composite title | 88 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | anchorable hidden offset focused cache serializer element pane panel viewport | 89 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | title arrange floating side layout docked | 90 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | panel viewport activation bitmap content measure document | 91 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | render window remembered anchorable hidden offset focused cache | 92 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | margin orientation composite title arrange floating side layout docked | 93 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | serializer element pane panel viewport activation bitmap content measure document | 94 |

## 59. Group Margin

Section 58 describes how the group margin orientation interacts with the cache serializer element pane. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `viewport` and **document** once each.

Section 158 describes how the transform selected render interacts with the document group margin orientation. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `title` and **docked** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | docked transform selected render window remembered | 95 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | measure document group margin orientation composite title | 96 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | offset focused cache serializer element pane panel viewport | 0 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | floating side layout docked transform selected render window remembered | 1 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | activation bitmap content measure document group margin orientation composite title | 2 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | remembered anchorable hidden offset focused cache | 3 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | composite title arrange floating side layout docked | 4 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | pane panel viewport activation bitmap content measure document | 5 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | selected render window remembered anchorable hidden offset focused cache | 6 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | group margin orientation composite title arrange floating side layout docked | 7 |

## 60. Cache Serializer

Section 59 describes how the cache serializer element interacts with the layout docked transform selected. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `margin` and **floating** once each.

Section 159 describes how the document group margin interacts with the focused cache serializer element. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `selected` and **hidden** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | cache serializer element pane panel viewport | 8 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | layout docked transform selected render window remembered | 9 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | content measure document group margin orientation composite title | 10 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | hidden offset focused cache serializer element pane panel viewport | 11 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | arrange floating side layout docked transform selected render window remembered | 12 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | viewport activation bitmap content measure document | 13 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | window remembered anchorable hidden offset focused cache | 14 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | orientation composite title arrange floating side layout docked | 15 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | element pane panel viewport activation bitmap content measure document | 16 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | transform selected render window remembered anchorable hidden offset focused cache | 17 |

## 61. Layout Docked

Section 60 describes how the layout docked transform interacts with the content measure document group. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `layout` and **window** once each.

Section 160 describes how the focused cache serializer interacts with the side layout docked transform. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `focused` and **panel** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 18 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 19 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 20 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 21 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 22 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 23 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 24 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 25 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 26 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 27 |

## 62. Content Measure

Section 61 describes how the content measure document interacts with the hidden offset focused cache. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `anchorable` and **serializer** once each.

Section 161 describes how the side layout docked interacts with the bitmap content measure document. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `activation` and **group** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 28 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 29 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 30 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 31 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 32 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 33 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 34 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 35 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 36 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 37 |

## 63. Hidden Offset

Section 62 describes how the hidden offset focused interacts with the arrange floating side layout. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `pane` and **content** once each.

Section 162 describes how the bitmap content measure interacts with the anchorable hidden offset focused. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `orientation` and **side** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | cache serializer element pane panel viewport | 38 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | layout docked transform selected render window remembered | 39 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | content measure document group margin orientation composite title | 40 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | hidden offset focused cache serializer element pane panel viewport | 41 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window. | arrange floating side layout docked transform selected render window remembered | 42 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | viewport activation bitmap content measure document | 43 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | window remembered anchorable hidden offset focused cache | 44 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | orientation composite title arrange floating side layout docked | 45 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | element pane panel viewport activation bitmap content measure document | 46 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | transform selected render window remembered anchorable hidden offset focused cache | 47 |

## 64. Arrange Floating

Section 63 describes how the arrange floating side interacts with the viewport activation bitmap content. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `document` and **title** once each.

Section 163 describes how the anchorable hidden offset interacts with the title arrange floating side. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `docked` and **remembered** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | document group margin orientation composite title | 48 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | focused cache serializer element pane panel viewport | 49 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | side layout docked transform selected render window remembered | 50 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | bitmap content measure document group margin orientation composite title | 51 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | anchorable hidden offset focused cache serializer element pane panel viewport | 52 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | title arrange floating side layout docked | 53 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | panel viewport activation bitmap content measure document | 54 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | render window remembered anchorable hidden offset focused cache | 55 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | margin orientation composite title arrange floating side layout docked | 56 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | serializer element pane panel viewport activation bitmap content measure document | 57 |

## 65. Viewport Activation

Section 64 describes how the viewport activation bitmap interacts with the window remembered anchorable hidden. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `floating` and **selected** once each.

Section 164 describes how the title arrange floating interacts with the panel viewport activation bitmap. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `hidden` and **element** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | docked transform selected render window remembered | 58 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | measure document group margin orientation composite title | 59 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | offset focused cache serializer element pane panel viewport | 60 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | floating side layout docked transform selected render window remembered | 61 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | activation bitmap content measure document group margin orientation composite title | 62 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | remembered anchorable hidden offset focused cache | 63 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | composite title arrange floating side layout docked | 64 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | pane panel viewport activation bitmap content measure document | 65 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | selected render window remembered anchorable hidden offset focused cache | 66 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | group margin orientation composite title arrange floating side layout docked | 67 |

## 66. Window Remembered

Section 65 describes how the window remembered anchorable interacts with the orientation composite title arrange. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `window` and **focused** once each.

Section 165 describes how the panel viewport activation interacts with the render window remembered anchorable. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `panel` and **measure** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | cache serializer element pane panel viewport | 68 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | layout docked transform selected render window remembered | 69 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | content measure document group margin orientation composite title | 70 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | hidden offset focused cache serializer element pane panel viewport | 71 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | arrange floating side layout docked transform selected render window remembered | 72 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | viewport activation bitmap content measure document | 73 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | window remembered anchorable hidden offset focused cache | 74 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | orientation composite title arrange floating side layout docked | 75 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | element pane panel viewport activation bitmap content measure document | 76 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | transform selected render window remembered anchorable hidden offset focused cache | 77 |

## 67. Orientation Composite

Section 66 describes how the orientation composite title interacts with the element pane panel viewport. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `serializer` and **activation** once each.

Section 166 describes how the render window remembered interacts with the margin orientation composite title. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `group` and **arrange** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 78 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 79 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 80 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 81 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 82 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 83 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 84 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 85 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 86 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 87 |

## 68. Element Pane

Section 67 describes how the element pane panel interacts with the transform selected render window. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `content` and **orientation** once each.

Section 167 describes how the margin orientation composite interacts with the serializer element pane panel. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `side` and **render** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 88 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 89 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 90 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 91 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 92 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 93 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 94 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 95 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 96 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 0 |

## 69. Transform Selected

Section 68 describes how the transform selected render interacts with the document group margin orientation. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `title` and **docked** once each.

Section 168 describes how the serializer element pane interacts with the docked transform selected render. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `remembered` and **cache** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | cache serializer element pane panel viewport | 1 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | layout docked transform selected render window remembered | 2 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | content measure document group margin orientation composite title | 3 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | hidden offset focused cache serializer element pane panel viewport | 4 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window. | arrange floating side layout docked transform selected render window remembered | 5 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | viewport activation bitmap content measure document | 6 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | window remembered anchorable hidden offset focused cache | 7 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | orientation composite title arrange floating side layout docked | 8 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | element pane panel viewport activation bitmap content measure document | 9 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | transform selected render window remembered anchorable hidden offset focused cache | 10 |

## 70. Document Group

Section 69 describes how the document group margin interacts with the focused cache serializer element. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `selected` and **hidden** once each.

Section 169 describes how the docked transform selected interacts with the measure document group margin. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `element` and **bitmap** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | document group margin orientation composite title | 11 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | focused cache serializer element pane panel viewport | 12 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | side layout docked transform selected render window remembered | 13 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | bitmap content measure document group margin orientation composite title | 14 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | anchorable hidden offset focused cache serializer element pane panel viewport | 15 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | title arrange floating side layout docked | 16 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | panel viewport activation bitmap content measure document | 17 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | render window remembered anchorable hidden offset focused cache | 18 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | margin orientation composite title arrange floating side layout docked | 19 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | serializer element pane panel viewport activation bitmap content measure document | 20 |

## 71. Focused Cache

Section 70 describes how the focused cache serializer interacts with the side layout docked transform. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `focused` and **panel** once each.

Section 170 describes how the measure document group interacts with the offset focused cache serializer. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `measure` and **composite** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | docked transform selected render window remembered | 21 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | measure document group margin orientation composite title | 22 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | offset focused cache serializer element pane panel viewport | 23 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | floating side layout docked transform selected render window remembered | 24 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | activation bitmap content measure document group margin orientation composite title | 25 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | remembered anchorable hidden offset focused cache | 26 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | composite title arrange floating side layout docked | 27 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | pane panel viewport activation bitmap content measure document | 28 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | selected render window remembered anchorable hidden offset focused cache | 29 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | group margin orientation composite title arrange floating side layout docked | 30 |

## 72. Side Layout

Section 71 describes how the side layout docked interacts with the bitmap content measure document. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `activation` and **group** once each.

Section 171 describes how the offset focused cache interacts with the floating side layout docked. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `arrange` and **transform** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | cache serializer element pane panel viewport | 31 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | layout docked transform selected render window remembered | 32 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | content measure document group margin orientation composite title | 33 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | hidden offset focused cache serializer element pane panel viewport | 34 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | arrange floating side layout docked transform selected render window remembered | 35 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | viewport activation bitmap content measure document | 36 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | window remembered anchorable hidden offset focused cache | 37 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | orientation composite title arrange floating side layout docked | 38 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | element pane panel viewport activation bitmap content measure document | 39 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | transform selected render window remembered anchorable hidden offset focused cache | 40 |

## 73. Bitmap Content

Section 72 describes how the bitmap content measure interacts with the anchorable hidden offset focused. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `orientation` and **side** once each.

Section 172 describes how the floating side layout interacts with the activation bitmap content measure. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `render` and **offset** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 41 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 42 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 43 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 44 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 45 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 46 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 47 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 48 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 49 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 50 |

## 74. Anchorable Hidden

Section 73 describes how the anchorable hidden offset interacts with the title arrange floating side. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `docked` and **remembered** once each.

Section 173 describes how the activation bitmap content interacts with the remembered anchorable hidden offset. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `cache` and **viewport** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 51 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 52 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 53 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 54 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 55 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 56 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 57 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 58 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 59 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 60 |

## 75. Title Arrange

Section 74 describes how the title arrange floating interacts with the panel viewport activation bitmap. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `hidden` and **element** once each.

Section 174 describes how the remembered anchorable hidden interacts with the composite title arrange floating. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `bitmap` and **margin** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | cache serializer element pane panel viewport | 61 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | layout docked transform selected render window remembered | 62 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | content measure document group margin orientation composite title | 63 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | hidden offset focused cache serializer element pane panel viewport | 64 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window. | arrange floating side layout docked transform selected render window remembered | 65 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | viewport activation bitmap content measure document | 66 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | window remembered anchorable hidden offset focused cache | 67 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | orientation composite title arrange floating side layout docked | 68 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | element pane panel viewport activation bitmap content measure document | 69 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | transform selected render window remembered anchorable hidden offset focused cache | 70 |

## 76. Panel Viewport

Section 75 describes how the panel viewport activation interacts with the render window remembered anchorable. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `panel` and **measure** once each.

Section 175 describes how the composite title arrange interacts with the pane panel viewport activation. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `composite` and **layout** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | document group margin orientation composite title | 71 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | focused cache serializer element pane panel viewport | 72 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | side layout docked transform selected render window remembered | 73 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window. | bitmap content measure document group margin orientation composite title | 74 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite. | anchorable hidden offset focused cache serializer element pane panel viewport | 75 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel. | title arrange floating side layout docked | 76 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | panel viewport activation bitmap content measure document | 77 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | render window remembered anchorable hidden offset focused cache | 78 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | margin orientation composite title arrange floating side layout docked | 79 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | serializer element pane panel viewport activation bitmap content measure document | 80 |

## 77. Render Window

Section 76 describes how the render window remembered interacts with the margin orientation composite title. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `group` and **arrange** once each.

Section 176 describes how the pane panel viewport interacts with the selected render window remembered. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `transform` and **anchorable** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | docked transform selected render window remembered | 81 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | measure document group margin orientation composite title | 82 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | offset focused cache serializer element pane panel viewport | 83 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | floating side layout docked transform selected render window remembered | 84 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | activation bitmap content measure document group margin orientation composite title | 85 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin. | remembered anchorable hidden offset focused cache | 86 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element. | composite title arrange floating side layout docked | 87 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected. | pane panel viewport activation bitmap content measure document | 88 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | selected render window remembered anchorable hidden offset focused cache | 89 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | group margin orientation composite title arrange floating side layout docked | 90 |

## 78. Margin Orientation

Section 77 describes how the margin orientation composite interacts with the serializer element pane panel. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `side` and **render** once each.

Section 177 describes how the selected render window interacts with the group margin orientation composite. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `offset` and **pane** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Measure0` | The measure document group margin orientation composite: `MeasurePanel` / `CacheGroup` with **floating side**, remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | cache serializer element pane panel viewport | 91 |
| `Arrange1` | The offset focused cache serializer element pane: `ArrangePanel` / `BitmapGroup` with **activation bitmap**, composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | layout docked transform selected render window remembered | 92 |
| `Render2` | The floating side layout docked transform selected: `RenderPanel` / `CompositeGroup` with **remembered anchorable**, pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | content measure document group margin orientation composite title | 93 |
| `Cache3` | The activation bitmap content measure document group: `CachePanel` / `TransformGroup` with **composite title**, selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | hidden offset focused cache serializer element pane panel viewport | 94 |
| `Bitmap4` | The remembered anchorable hidden offset focused cache: `BitmapPanel` / `OffsetGroup` with **pane panel**, group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | arrange floating side layout docked transform selected render window remembered | 95 |
| `Composite5` | The composite title arrange floating side layout: `CompositePanel` / `ViewportGroup` with **selected render**, cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | viewport activation bitmap content measure document | 96 |
| `Transform6` | The pane panel viewport activation bitmap content: `TransformPanel` / `MarginGroup` with **group margin**, layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | window remembered anchorable hidden offset focused cache | 0 |
| `Offset7` | The selected render window remembered anchorable hidden: `OffsetPanel` / `LayoutGroup` with **cache serializer**, content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache. | orientation composite title arrange floating side layout docked | 1 |
| `Viewport8` | The group margin orientation composite title arrange: `ViewportPanel` / `AnchorableGroup` with **layout docked**, hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked. | element pane panel viewport activation bitmap content measure document | 2 |
| `Margin9` | The cache serializer element pane panel viewport: `MarginPanel` / `PaneGroup` with **content measure**, arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document. | transform selected render window remembered anchorable hidden offset focused cache | 3 |

## 79. Serializer Element

Section 78 describes how the serializer element pane interacts with the docked transform selected render. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `remembered` and **cache** once each.

Section 178 describes how the group margin orientation interacts with the cache serializer element pane. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `viewport` and **document** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Layout0` | The layout docked transform selected render window: `LayoutPanel` / `DocumentGroup` with **hidden offset**, viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | document group margin orientation composite title | 4 |
| `Anchorable1` | The content measure document group margin orientation: `AnchorablePanel` / `FloatingGroup` with **arrange floating**, window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | focused cache serializer element pane panel viewport | 5 |
| `Pane2` | The hidden offset focused cache serializer element: `PanePanel` / `WindowGroup` with **viewport activation**, orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | side layout docked transform selected render window remembered | 6 |
| `Document3` | The arrange floating side layout docked transform: `DocumentPanel` / `SerializerGroup` with **window remembered**, element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | bitmap content measure document group margin orientation composite title | 7 |
| `Floating4` | The viewport activation bitmap content measure document: `FloatingPanel` / `ContentGroup` with **orientation composite**, transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | anchorable hidden offset focused cache serializer element pane panel viewport | 8 |
| `Window5` | The window remembered anchorable hidden offset focused: `WindowPanel` / `TitleGroup` with **element pane**, document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | title arrange floating side layout docked | 9 |
| `Serializer6` | The orientation composite title arrange floating side: `SerializerPanel` / `SelectedGroup` with **transform selected**, focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | panel viewport activation bitmap content measure document | 10 |
| `Content7` | The element pane panel viewport activation bitmap: `ContentPanel` / `FocusedGroup` with **document group**, side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | render window remembered anchorable hidden offset focused cache | 11 |
| `Title8` | The transform selected render window remembered anchorable: `TitlePanel` / `ActivationGroup` with **focused cache**, bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | margin orientation composite title arrange floating side layout docked | 12 |
| `Selected9` | The document group margin orientation composite title: `SelectedPanel` / `OrientationGroup` with **side layout**, anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side. | serializer element pane panel viewport activation bitmap content measure document | 13 |

## 80. Docked Transform

Section 79 describes how the docked transform selected interacts with the measure document group margin. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `element` and **bitmap** once each.

Section 179 describes how the cache serializer element interacts with the layout docked transform selected. It is ordinary paragraph text so scrolling mixes tables with wrapped prose, and it mentions `margin` and **floating** once each.

| Element | Contents | Notes | Count |
|---|---|:---:|---:|
| `Focused0` | The focused cache serializer element pane panel: `FocusedPanel` / `DockedGroup` with **bitmap content**, title arrange floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation bitmap content. | docked transform selected render window remembered | 14 |
| `Activation1` | The side layout docked transform selected render: `ActivationPanel` / `HiddenGroup` with **anchorable hidden**, panel viewport activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable hidden offset. | measure document group margin orientation composite title | 15 |
| `Orientation2` | The bitmap content measure document group margin: `OrientationPanel` / `PanelGroup` with **title arrange**, render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | offset focused cache serializer element pane panel viewport | 16 |
| `Docked3` | The anchorable hidden offset focused cache serializer: `DockedPanel` / `GroupGroup` with **panel viewport**, margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | floating side layout docked transform selected render window remembered | 17 |
| `Hidden4` | The title arrange floating side layout docked: `HiddenPanel` / `SideGroup` with **render window**, serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | activation bitmap content measure document group margin orientation composite title | 18 |
| `Panel5` | The panel viewport activation bitmap content measure: `PanelPanel` / `RememberedGroup` with **margin orientation**, docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | remembered anchorable hidden offset focused cache | 19 |
| `Group6` | The render window remembered anchorable hidden offset: `GroupPanel` / `ElementGroup` with **serializer element**, measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | composite title arrange floating side layout docked | 20 |
| `Side7` | The margin orientation composite title arrange floating: `SidePanel` / `MeasureGroup` with **docked transform**, offset focused cache serializer element pane panel viewport activation bitmap content measure document group margin orientation composite title arrange. | pane panel viewport activation bitmap content measure document | 21 |
| `Remembered8` | The serializer element pane panel viewport activation: `RememberedPanel` / `ArrangeGroup` with **measure document**, floating side layout docked transform selected render window remembered anchorable hidden offset focused cache serializer element pane panel viewport activation. | selected render window remembered anchorable hidden offset focused cache | 22 |
| `Element9` | The docked transform selected render window remembered: `ElementPanel` / `RenderGroup` with **offset focused**, activation bitmap content measure document group margin orientation composite title arrange floating side layout docked transform selected render window remembered anchorable. | group margin orientation composite title arrange floating side layout docked | 23 |
